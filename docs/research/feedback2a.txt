One more thing: if you could refer to the CPU/isa as "MakerLisp COR24", and direct people to my site, that would be appreciated. I may make "MakerLisp COR24" a trademark, and the only requirement is that an implementation be a compatible (same behavior) implementation to use the name "MakerLisp COR24" (I can't trademark COR24, it's too generic, it wouldn't hold up - but having my company name in there, which is already trademarked, will be sufficient distinction and protection). I won't charge anyone any money, but I want to review their implementation, to make sure it works the same if they're going to call it by that name. That way, you know what the processor is when you use that name. If someone takes my implementation and makes it do something else, then COR24 wouldn't tell you for sure what the thing really is. Likewise, that;s why it's important to get the information right before you publish it.

